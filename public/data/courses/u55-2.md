:::qchain{title="问题链闯关：先把客户关系的数据底座搭稳"}

:::question{title="小北的第一周：客户关系管理，为什么先搭底座？", hint="先凭直觉想：AI 打标签、RFM 分层都跑不起来，是不是因为底下的表没建对？"}
:::scene{
  setup="小北转岗到客户服务与体验组，mentor 说：投诉不是麻烦，是生意在'漏水'的地方。 这周的真实任务就是：搭好客户关系的数据底座。"
  speaker="mentor"
  line="别把它当课本。做完这个任务，你要能独立回答一个问题——怎么把客户与订单的数据底座搭稳？"
  focus="数据底座搭建：从现象到决策"
  focusQ="怎么把客户与订单的数据底座搭稳？"
  thought="小北盯着屏幕，心里犯嘀咕：底座不搭稳，后面的 AI 打标签和 RFM 分层都是空中楼阁。"
  revealDelay=0.6
  stagger=0.8
  duration=0.95
  trigger="0px 0px -22% 0px"
}

mentor 说：AI 打标签、RFM 分层跑不起来，多半是底座没搭对。先凭直觉答一句——为什么非得先把表和关联建好？

:::reveal{title="看解析：为什么先搭数据底座"}
> 客服关系管理，底座先搭稳。小北打开飞书，先把「焕新家电-客户管理系统」这张多维表格建起来——表结构、字段、表间关联，一步都不能错。

把客户信息与订单拆成两张表，再用关联字段把它们串起来，是后面所有 AI 打标签、RFM 分层能跑起来的前提。

:::cards{
  title="数据底座为何是前提"
  theme="violet"
  items=[
    {title:"两表分开", desc:"客户信息与订单拆成两张表，各自清晰，避免一张大表难以维护。"},
    {title:"关联串起", desc:"用关联字段把订单指向客户，让每一次下单自动回流到客户档案。"},
    {title:"支撑上层", desc:"关联与汇总配好后，AI 打标签、RFM 分层才能实时算出等级与消费。"}
  ]
}
:::
:::checkpoint{
  type="multiple_choice"
  scenario="mentor 想确认小北懂不懂『底座优先』的道理。"
  question="关于「为什么先搭数据底座」，下列说法正确的是？"
  options=["把客户信息与订单拆成两张表、用关联字段串起来，是后面 AI 打标签、RFM 分层能跑起来的前提", "数据底座只需一张『客户表』即可，订单数据无需单独建表", "表间关联是可选的，不影响后续 AI 打标签与 RFM 分层", "先搭 AI 打标签与 RFM 分层，再补数据底座也不迟"]
  answer="把客户信息与订单拆成两张表、用关联字段串起来，是后面 AI 打标签、RFM 分层能跑起来的前提"
  hints=["先有表与关联，上层才能算。", "『拆表+关联』对应什么目的？"]
  feedback="对——底座是前提，表结构和关联没搭好，上面的智能都跑不动。"
  unlock="那具体怎么一步步搭起来？"
}
:::

:::question{title="从建表到表间联动，到底走哪七步？", hint="先猜：新建表、导数据、做关联、配汇总……这七步大概怎么排？"}
mentor 给了张空白多维表格，让小北照七步法搭出「焕新家电-客户管理系统」。先凭直觉排一下这七步。

:::reveal{title="看解析：数据底座搭建七步法"}
把客户信息与订单拆成两张表，再用关联字段串起来，是后面 AI 打标签、RFM 分层能跑起来的前提。

:::steps{
  title="数据底座搭建七步法"
  steps=[
    {title:"新建多维表格", desc:"进入飞书工作台 → 多维表格 → 新建，命名为「焕新家电-客户管理系统」，依次创建「客户基础信息表」与「订单数据表」。"},
    {title:"导入 Excel 数据", desc:"导入 excel 数据，系统自动创建以上两张表的结构与内容。"},
    {title:"订单表关联客户", desc:"在「订单数据表」新增「涉及客户」字段，单项关联「客户基础信息表」，让订单能指向具体客户。（图5-14）"},
    {title:"订单表引用姓名", desc:"新增「客户姓名」字段，查找引用客户基础信息表的「姓名」字段，查询条件设为「客户ID」等于「客户ID」。（图5-15）"},
    {title:"客户表关联订单", desc:"「客户基础信息表」新增「累计消费金额」字段，并新增「单向关联」字段，单项关联订单表。（图5-16）"},
    {title:"客户表汇总金额", desc:"查找引用订单记录表的「金额」字段，查询条件「客户ID」等于「客户ID」，计算方式设为「求和」。（图5-17）"},
    {title:"验证联动", desc:"新增一条订单信息，客户基础信息表中该客户的累计消费金额应自动更新。（图5-18）"}
  ]
}
:::
:::checkpoint{
  type="multiple_choice"
  scenario="mentor 让小北判断：订单表怎么关联到客户？"
  question="关于「数据底座搭建七步法」，下列说法正确的是？"
  options=["在「订单数据表」新增「涉及客户」字段单项关联「客户基础信息表」，让订单能指向具体客户", "导入 Excel 后需手动逐个输入字段，系统不会自动创建表结构与内容", "客户表汇总金额指人工累加订单金额后填回客户表，而非查找引用求和", "验证联动指新增订单后需手动刷新，累计消费金额不会自动更新"]
  answer="在「订单数据表」新增「涉及客户」字段单项关联「客户基础信息表」，让订单能指向具体客户"
  hints=["订单要能『指向』哪个客户。", "关联是单向还是双向？"]
  feedback="准确——订单表单项关联客户表，是让下单自动回流的关键一步。"
  unlock="两张表分别有哪些字段？先看客户表。"
}
:::

:::question{title="客户基础信息表，该配哪些字段？", hint="先想：要支撑 AI 打标签和 RFM，客户表最少得记下哪些信息？"}
小北要设计「客户基础信息表」。先凭直觉列一列：一个客户从 ID 到消费汇总，最少该有哪些字段？

:::reveal{title="看解析：客户基础信息表字段设计"}
两张表的字段设计如下，作为后续录入与关联的配置基准。

| 字段名称 | 字段类型 | 配置说明 |
| --- | --- | --- |
| 客户ID | 自动编号 | 自动生成，格式：KH2024+四位序号 |
| 姓名 | 文本 | 单行文本，必填项 |
| 手机号 | 电话号码 | 自动识别手机号格式，唯一性校验 |
| 来源渠道 | 单选 | 选项：京东旗舰店、天猫旗舰店、抖音直播、门店自访、老客户推荐、美团/大众点评 |
| 首次购买日期 | 日期 | 格式：YYYY-MM-DD |
| 购买产品类型 | 多选 | 选项：冰箱、洗衣机、空调、小家电、全屋套餐 |
| 客户等级 | 单选 | 选项：A类（高价值）、B类（潜力）、C类（普通）、D类（新客户） |
| 累计消费金额 | 数字 | 关联订单表自动汇总，保留2位小数 |
| 最近购买日期 | 日期 | 关联订单表自动提取最新订单日期 |
| 复购次数 | 数字 | 统计关联订单数量 |

注意：客户等级用单选（A/B/C/D），累计消费金额是数字、关联订单表自动汇总。
:::
:::checkpoint{
  type="multiple_choice"
  scenario="mentor 出了一道字段匹配题。"
  question="关于「客户基础信息表字段设计」，下列说法正确的是？"
  options=["客户ID为自动编号（格式KH2024+四位序号），累计消费金额为数字、关联订单表自动汇总保留2位小数", "客户ID需手动输入，无自动编号规则", "累计消费金额由人工每月统计后填回，不关联订单表", "客户等级字段为文本，可随意填写A/B/C/D"]
  answer="客户ID为自动编号（格式KH2024+四位序号），累计消费金额为数字、关联订单表自动汇总保留2位小数"
  hints=["客户ID 怎么生成？看格式 KH2024。", "累计消费金额从哪来？"]
  feedback="准确——自动编号 + 自动汇总，客户表的字段就配对了。"
  unlock="再看订单表有哪些字段。"
}
:::

:::question{title="订单数据表，又该配哪些字段？", hint="先想：一笔订单要能反查到客户、算金额，最少该记哪些字段？"}
小北接着设计「订单数据表」。先凭直觉答：一笔订单要能反查客户、记金额，最少该有哪些字段？

:::reveal{title="看解析：订单数据表字段设计"}
| 字段名称 | 字段类型 | 配置说明 |
| --- | --- | --- |
| 订单号 | 自动编号 | 格式：DD202401+三位序号 |
| 客户ID | 关联记录 | 关联"客户基础信息表"，显示客户姓名 |
| 产品名称 | 文本 | 单行文本，如：海尔冰箱BCD-520W |
| 品牌 | 单选 | 选项：海尔、格力、美的、小天鹅、西门子、戴森、九阳、苏泊尔等 |
| 产品分类 | 单选 | 选项：冰箱、洗衣机、空调、小家电、全屋套餐 |
| 金额（元） | 数字 | 货币格式，保留2位小数 |
| 购买时间 | 日期+时间 | 精确到分钟 |
| 支付状态 | 单选 | 选项：已支付、待支付、已退款 |
| 发票状态 | 单选 | 选项：已开票、待开票、未开票 |

注意：订单表客户ID是关联记录（显示客户姓名），金额是货币格式保留2位小数。
:::
:::checkpoint{
  type="multiple_choice"
  scenario="mentor 出了一道订单表字段题。"
  question="关于「订单数据表字段设计」，下列说法正确的是？"
  options=["客户ID为关联记录（关联客户基础信息表、显示客户姓名），金额为数字货币格式保留2位小数", "订单表客户ID为普通文本，不关联客户基础信息表", "金额字段为文本，无需货币格式", "支付状态为多选，可同时选已支付与已退款"]
  answer="客户ID为关联记录（关联客户基础信息表、显示客户姓名），金额为数字货币格式保留2位小数"
  hints=["订单要能反查哪个客户。", "金额用什么格式？"]
  feedback="准确——关联记录 + 货币格式，订单表字段就配齐了。"
  unlock="表建好、关联好，怎么证明它真的通了？"
}
:::

:::question{title="怎么验证底座真的搭通了？", hint="先猜：增一张订单，客户表的哪个数字应该自己变？"}
小北搭完两张表和关联，mentor 让他验证。先凭直觉答：新增一条订单后，客户表哪个数字会自己动？

:::reveal{title="看解析：验证联动"}
> 关联搭好后，订单与客户的每一次变动都会自动回流——这正是后面客户等级、累计消费能实时计算的关键。

验证联动：新增一条订单信息，客户基础信息表中该客户的累计消费金额应自动更新。

:::cards{
  title="联动回流的意义"
  theme="mint"
  items=[
    {title:"自动更新", desc:"新增订单，客户表累计消费金额自动加总，无需手工改。"},
    {title:"实时计算", desc:"客户等级、累计消费依赖联动数据，才能实时算出结果。"},
    {title:"闭环验证", desc:"若金额不自动更新，说明关联或汇总配置出错，需回查。"}
  ]
}
:::
:::checkpoint{
  type="multiple_choice"
  scenario="mentor 让小北描述验证联动的现象。"
  question="关于「验证联动」，下列说法正确的是？"
  options=["新增一条订单信息，客户基础信息表中该客户的累计消费金额应自动更新", "新增订单后需手动改客户表的累计消费金额，系统不会自动回流", "关联只在客户表指向订单表，订单表无法指向客户", "累计消费金额固定不变，与新增订单无关"]
  answer="新增一条订单信息，客户基础信息表中该客户的累计消费金额应自动更新"
  hints=["订单变动要『回流』到客户。", "金额是自动求和还是手填？"]
  feedback="准确——金额自动更新，说明底座真的通了，上层智能才有数据源。"
  unlock="最后一关，把整件事串起来考。"
}
:::

:::question{title="转正小考：数据底座搭建，你真搭明白了吗？", hint="先快速在脑中过一遍全任务，再答题"}
走完前面五关，把「拆两表 → 七步搭建 → 字段设计 → 验证联动」串成一条线——下面这道综合题，考你到底学没学透。

:::reveal{title="考前锦囊：一张图回顾全任务"}
> 回顾主线：**拆两张表（客户/订单）→ 七步搭建（新建多维表格→导入Excel→订单关联客户→引用姓名→客户关联订单→汇总金额→验证联动）→ 字段设计（客户表自动编号/单选等级/自动汇总，订单表关联记录/货币金额）→ 联动自动更新**。把这条线在脑中过一遍，再作答。

:::steps{
  title="本任务知识主线（自检清单）"
  theme="coral"
  steps=[
    {title:"拆两表", desc:"客户信息与订单拆两张表，用关联字段串起来，是上层智能的前提。"},
    {title:"七步搭建", desc:"新建多维表格→导入Excel→订单关联客户→引用姓名→客户关联订单→汇总金额→验证联动。"},
    {title:"字段设计", desc:"客户ID自动编号KH2024+四位；订单客户ID为关联记录显示姓名；金额货币格式。"},
    {title:"验证联动", desc:"新增订单，客户表累计消费金额自动更新，说明底座已通。"}
  ]
}
:::
:::challenge{
  id="u55-2_c1"
  type="multiple_choice"
  title="转正小考"
  scenario="mentor 出了一道综合题，考你这个任务到底学没学透。"
  question="关于「数据底座搭建」，下列说法正确的是？"
  options=["把客户信息与订单拆成两张表、用关联字段串起来，并配置字段类型与汇总规则，是后续 AI 打标签与 RFM 分层的前提", "数据底座只需一张表，关联字段纯属多余", "累计消费金额由人工统计填回，无需关联订单表自动汇总", "客户ID用文本手动维护即可，无需自动编号"]
  answer="把客户信息与订单拆成两张表、用关联字段串起来，并配置字段类型与汇总规则，是后续 AI 打标签与 RFM 分层的前提"
  hints=["把本任务的知识点串起来想。", "正确答案就在你刚学的内容里。"]
  feedback="答对了，这个任务的核心你已经掌握——底座稳，上层智能才跑得动。"
  unlock="下一关，你会遇到更复杂的真实场景。"
}

> 学完这一单元，回到课程页可以看到你的「学习增益」——课前测与课后测的对比，就是你的成长曲线。
:::
:::
